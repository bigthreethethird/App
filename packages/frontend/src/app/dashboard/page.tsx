import {UserButton} from '@clerk/nextjs'; import {currentUser} from '@clerk/nextjs/server';
export default async function Dashboard(){const user=process.env.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY?await currentUser():null; const name=user?.firstName||'your brand'; return <div className="p-5 md:p-10"><div className="mb-12 flex items-center justify-between lg:hidden"><span className="text-xl font-semibold">cannect</span><UserButton /></div><p className="text-xs font-bold uppercase tracking-[.12em] text-accent">Overview</p><h1 className="mt-3 text-4xl font-semibold">Good morning, {name}</h1><p className="mt-3 text-text-secondary">Your product passport workspace is ready.</p><div className="mt-10 grid gap-6 md:grid-cols-3"><div className="glass rounded-2xl p-6"><p className="text-sm text-text-secondary">Total scans</p><p className="mt-3 text-3xl font-semibold">—</p><p className="mt-2 text-sm text-text-muted">Your first scan will appear here</p></div><div className="glass rounded-2xl p-6"><p className="text-sm text-text-secondary">Products live</p><p className="mt-3 text-3xl font-semibold">0</p></div><div className="glass rounded-2xl p-6"><p className="text-sm text-text-secondary">Verification rate</p><p className="mt-3 text-3xl font-semibold">—</p></div></div></div>}
