'use client'; import {useEffect,useState} from 'react'; import {useParams} from 'next/navigation'; import ProductEditor from '../new/page'; import {api} from '@/lib/api'; export default function Edit(){const {id}=useParams<{id:string}>(); const [ready,setReady]=useState(false); useEffect(()=>{api.product(id).then(()=>setReady(true)).catch(()=>setReady(true))},[id]); return ready?<ProductEditor/>:<p>Loading…</p>}
