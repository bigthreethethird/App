'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { api } from '@/lib/api';
export default function ProductEditor(){ const router=useRouter(); const [name,setName]=useState(''); const [description,setDescription]=useState(''); const [busy,setBusy]=useState(false); const [error,setError]=useState(''); async function save(published:boolean){setBusy(true);setError('');try{await api.createProduct({name,description,published});router.push('/dashboard/products')}catch(e){setError(e instanceof Error?e.message:'Unable to save product')}finally{setBusy(false)}} return <form onSubmit={e=>{e.preventDefault();save(false)}}><h1 className="text-4xl font-semibold">Create product</h1>{error&&<p className="mt-5 text-sm text-red-300">{error}</p>}<div className="glass mt-10 grid gap-6 rounded-2xl p-6"><label>Product name<input required value={name} onChange={e=>setName(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-white/[.045] px-3"/></label><label>Description<textarea value={description} onChange={e=>setDescription(e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border border-white/10 bg-white/[.045] p-3"/></label><div className="flex gap-3"><button disabled={busy} type="submit">{busy?'Saving…':'Save draft'}</button><button disabled={busy} type="button" onClick={()=>save(true)}>Publish</button></div></div></form>}
