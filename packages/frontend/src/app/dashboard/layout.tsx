'use client'; import {useState} from 'react'; import Link from 'next/link'; import {UserButton} from '@clerk/nextjs';
const nav=[['Overview','/dashboard','⌂'],['Products','/dashboard/products','▣'],['Analytics','/dashboard/analytics','⌁'],['Settings','/dashboard/settings','⚙'],['Billing','/dashboard/settings','◇']];
export default function DashboardLayout({children}:{children:React.ReactNode}){const [open,setOpen]=useState(false);return <div className="min-h-dvh bg-canvas-raised"><aside className={`fixed inset-y-0 left-0 z-30 w-64 border-r border-white/10 bg-[#111017] p-5 transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex items-center justify-between"><strong className="text-xl">cannect</strong><button className="lg:hidden" onClick={()=>setOpen(false)} aria-label="Close menu">×</button></div><div className="mt-8 rounded-xl bg-white/[.04] p-3"><p className="text-xs text-text-muted">WORKSPACE</p><p className="mt-1 text-sm font-semibold">Your brand</p></div><nav className="mt-8 space-y-1">{nav.map(([label,url,icon])=><Link onClick={()=>setOpen(false)} className="flex min-h-11 items-center gap-3 rounded-xl px-3 text-sm text-text-secondary hover:bg-white/[.06] hover:text-text-primary" href={url} key={label}><span className="text-accent">{icon}</span>{label}</Link>)}</nav><div className="absolute bottom-5 left-5 right-5 flex items-center gap-3 border-t border-white/10 pt-5"><div className="h-9 w-9 rounded-full bg-accent/20 text-center leading-9 text-accent">Y</div><span className="text-sm text-text-secondary">Your brand</span></div></aside><div className="lg:pl-64"><header className="flex h-20 items-center justify-between border-b border-white/10 px-5 md:px-8"><button onClick={()=>setOpen(true)} className="h-11 w-11 rounded-xl border border-white/10 lg:hidden" aria-label="Open menu">☰</button><span className="text-sm text-text-muted">Brand workspace</span><UserButton/></header><main className="mx-auto max-w-7xl p-5 md:p-8">{children}</main></div></div>}
