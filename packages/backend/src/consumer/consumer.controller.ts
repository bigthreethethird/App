import {Body,Controller,Get,Post,Req,UnauthorizedException} from '@nestjs/common'; @Controller('api/consumer') export class ConsumerController{@Post('auth') auth(@Body() body:{token?:string}){return {token:body.token||'consumer-demo-token',consumer:{id:'consumer-demo'}}} @Get('me') me(@Req() req:any){if(!req.headers.authorization)throw new UnauthorizedException();return {id:'consumer-demo'}}}
