import {BadRequestException,ConflictException,Injectable} from '@nestjs/common'; import {PrismaService} from '../prisma/prisma.service';
@Injectable() export class WaitlistService{constructor(private prisma:PrismaService){} async create(email:string,brandName?:string,consent?:boolean){const normalized=email?.trim().toLowerCase();if(!normalized||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(normalized))throw new BadRequestException('A valid email is required');if(consent!==true)throw new BadRequestException('Consent is required');if(await this.prisma.waitlistSignup.findUnique({where:{email:normalized}}))throw new ConflictException('Email is already on the waitlist');return this.prisma.waitlistSignup.create({data:{email:normalized,brandName:brandName?.trim()||undefined,consent:true}})}}
