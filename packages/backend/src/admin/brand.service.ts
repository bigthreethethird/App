import { Injectable, NotFoundException } from '@nestjs/common'; import { PrismaService } from '../prisma/prisma.service';
@Injectable() export class BrandService { constructor(private readonly prisma:PrismaService){} private async brand(uid:string){const b=await this.prisma.brand.findUnique({where:{clerkId:uid}});if(!b)throw new NotFoundException('Brand not found');return b;} get(uid:string){return this.brand(uid)} async update(uid:string,body:{name?:string;slug?:string}){const b=await this.brand(uid);return this.prisma.brand.update({where:{id:b.id},data:{name:body.name}})} }
