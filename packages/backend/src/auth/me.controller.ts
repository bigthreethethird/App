import {Controller,Get,Req,UseGuards,UnauthorizedException} from '@nestjs/common'; import {ClerkAuthGuard} from './auth.guard'; @Controller('api') export class MeController{@Get('me') @UseGuards(ClerkAuthGuard) me(@Req() req:any){if(process.env.CLERK_SECRET_KEY&&!req.headers.authorization)throw new UnauthorizedException();return {id:req.auth?.userId||'demo-brand',name:'Your brand'}}}
